#!/usr/bin/env bash
source bin/environments)" ]; then
    echo "You have no running container"
else
    echo "Stopping all running containers..."
    docker ps -q | while IFS='' read -r container; do
        docker stop "$container"
    done
fi
